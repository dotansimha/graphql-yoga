import { RemoteContent } from 'nextra/components'
import { Callout, LegacyPackageCmd, mdxComponents, Tabs } from '@theguild/components'

export { getStaticPaths, getStaticProps } from '../../lib/v4-get-static-props'

import CodegenCallout from '../../components/codegen-callout.mdx'

<RemoteContent
  components={{
    ...mdxComponents,
    Callout,
    PackageCmd: LegacyPackageCmd,
    Tab: Tabs.Tab,
    Tabs,
    $Tab: Tabs.Tab,
    $Tabs: Tabs,
    CodegenCallout
  }}
/>
