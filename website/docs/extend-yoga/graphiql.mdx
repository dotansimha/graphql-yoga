---
id: graphiql
title: GraphiQL Playground
sidebar_label: GraphiQL
---

[GraphiQL is an in-browser IDE for writing, validating, and testing GraphQL queries.](https://github.com/graphql/graphiql)

You can configure GraphiQL or disable it completely with the `graphiql` option. By default, GraphQL Yoga enables GraphiQL in development mode.

## Example

```ts
import { GraphQLServer } from 'graphql-yoga'

// Provide your schema
const server = new GraphQLServer({
  typeDefs: /* GraphQL */ `
    type Query {
      greetings: String!
    }
  `,
  resolvers: {
    Query: {
      greetings: () => 'Hello World!',
    },
  },
  graphiql: {
    // See all options here: https://github.com/graphql/graphiql/tree/main/packages/graphiql
    defaultQuery: /* GraphQL */ `
      query {
        greetings
      }
    `,
  },
})
// Start the server and explore http://localhost:4000/graphql
server.start()
```
