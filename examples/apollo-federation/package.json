{
  "name": "example-apollo-federation",
  "version": "0.0.1",
  "private": true,
  "workspaces": [
    "service",
    "gateway"
  ],
  "scripts": {
    "check": "exit 0",
    "generate-supergraph": "node scripts/generate-supergraph.js",
    "start": "concurrently \"pnpm start:service\" \"pnpm start:gateway\"",
    "start:gateway": "cd gateway && pnpm start",
    "start:service": "cd service && pnpm start"
  },
  "dependencies": {
    "concurrently": "^8.0.0"
  },
  "devDependencies": {
    "@graphql-tools/executor-http": "1.0.2",
    "@graphql-tools/federation": "1.1.11",
    "@theguild/federation-composition": "0.1.3"
  }
}
