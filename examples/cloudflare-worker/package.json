{
  "name": "cloudflare-worker",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "check": "tsc --pretty --noEmit"
  },
  "devDependencies": {
    "esbuild": "^0.13.13",
    "typescript": "^4.4.4",
    "tslib": "2.3.1"
  }
}
