{
  "name": "service-worker",
  "version": "0.12.6",
  "private": true,
  "scripts": {
    "build": "wrangler publish --dry-run --outdir=dist",
    "start": "wrangler dev",
    "test": "jest",
    "check": "tsc --pretty --noEmit"
  },
  "dependencies": {
    "@graphql-yoga/common": "2.12.6",
    "graphql": "16.5.0"
  },
  "devDependencies": {
    "typescript": "4.7.4",
    "wrangler": "2.0.25"
  }
}
