{
  "name": "service-worker",
  "version": "0.8.0",
  "private": true,
  "scripts": {
    "build": "wrangler publish --dry-run --outdir=dist",
    "start": "wrangler dev",
    "test": "jest",
    "check": "tsc --pretty --noEmit"
  },
  "dependencies": {
    "@graphql-yoga/common": "2.8.0",
    "graphql": "16.3.0"
  },
  "devDependencies": {
    "typescript": "4.5.5",
    "wrangler": "2.0.6"
  }
}
