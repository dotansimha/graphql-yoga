{
  "name": "service-worker",
  "version": "0.12.12",
  "private": true,
  "scripts": {
    "build": "wrangler publish --dry-run --outdir=dist",
    "start": "wrangler dev",
    "test": "jest",
    "check": "tsc --pretty --noEmit"
  },
  "dependencies": {
    "@graphql-yoga/common": "2.12.12",
    "graphql": "16.6.0"
  },
  "devDependencies": {
    "typescript": "4.8.4",
    "wrangler": "2.1.13"
  }
}
