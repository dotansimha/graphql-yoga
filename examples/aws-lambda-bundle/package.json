{
  "name": "aws-lambda-bundle",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node build.js",
    "check": "tsc --pretty --noEmit"
  },
  "devDependencies": {
    "esbuild": "0.15.4",
    "typescript": "4.7.4",
    "tslib": "2.4.0"
  }
}
