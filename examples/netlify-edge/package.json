{
  "name": "example-netlify-edge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node ./build.js",
    "check": "exit 0",
    "start": "pnpm build && netlify dev"
  },
  "dependencies": {
    "graphql": "16.10.0",
    "graphql-yoga": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "0.24.0",
    "netlify-cli": "^17.0.0"
  }
}
