{
  "name": "example-netlify-edge",
  "version": "0.0.0",
  "private": true,
  "scripts": {
    "build": "node ./build.js",
    "check": "exit 0",
    "start": "pnpm build && netlify dev"
  },
  "dependencies": {
    "graphql": "16.9.0",
    "graphql-yoga": "workspace:*"
  },
  "devDependencies": {
    "esbuild": "0.17.19",
    "netlify-cli": "^16.0.0"
  }
}
